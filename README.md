# Cursor-Based-Paint-System-benefiting-Timer-Interrupts-in-Assembly-X86
The project implements a simulation of user input using mouse courser and implements interrupt routines based on user inputs, it represents these inputs such as color, direction, and location as numerical numbers in memory values.
